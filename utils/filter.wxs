var filters = {
  toFix: function (value) {
    if(typeof(value)=='undefined'){
        return value
    }else{
    //此处2为保留两位小数
    return value.toFixed(2)
    }
  }
}
module.exports = {
  toFix: filters.toFix
}